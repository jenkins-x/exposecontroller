apiVersion: "v1"
data:
  config.yml: |-
    domain: 
    exposer: 
    # exposer: LoadBalancer
    # exposer: Ingress
    # exposer: Route
    # exposer: NodePort
kind: "ConfigMap"
metadata:
  labels:
    provider: "fabric8"
    project: "exposecontroller"
  name: "exposecontroller"
