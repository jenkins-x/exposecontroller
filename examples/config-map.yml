apiVersion: "v1"
data:
  config.yml: |-
    domain: replace.me.io
    exposer: Ingress
    # exposer: LoadBalancer
    # exposer: NodePort
    # exposer: Route
kind: "ConfigMap"
metadata:
  labels:
    provider: "fabric8"
    project: "exposecontroller"
  name: "exposecontroller-cm"
