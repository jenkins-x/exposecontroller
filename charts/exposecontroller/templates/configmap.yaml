{{- if .Values.config }}
apiVersion: v1
data:
  config.yml: |-
{{- if .Values.config.exposer }}
    exposer: {{ .Values.config.exposer  }}
{{- end }}
{{- if .Values.config.domain }}
    domain: {{ .Values.config.domain }}
{{- end }}
    http: {{ .Values.config.http | quote }}
    tls-acme: {{ .Values.config.tlsacme | quote  }}
kind: ConfigMap
metadata:
  name: exposecontroller
{{- end }}